interface DAnimeRow {
  id: string;
  title: string;
  type: string;
  episodes: number;
  status: string;
  year: number;
  image_url: string;
  name: string;
  watch_status: string;
}

interface DTagRow {
  id: number;
  name: string;
}
